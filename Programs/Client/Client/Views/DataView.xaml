<UserControl x:Class="CarCRUD.Views.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarCRUD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="860">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <TextBlock Text="Create Car Brand"
                       FontSize="30"
                       Style="{StaticResource TitleTheme}"/>
        </Border>
        
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <TextBox x:Name="BrandName"   
                     Height="40"
                     Width="300"/>
            <Button x:Name="Submit"
                    Content="SUBMIT"
                    Height="40"
                    Width="100"
                    Style="{StaticResource LoginButtonTheme}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
